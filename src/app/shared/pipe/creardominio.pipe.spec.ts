import { CreardominioPipe } from './creardominio.pipe';

describe('CreardominioPipe', () => {
  it('create an instance', () => {
    const pipe = new CreardominioPipe();
    expect(pipe).toBeTruthy();
  });
});
